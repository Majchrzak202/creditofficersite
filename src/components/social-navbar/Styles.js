import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles({
  item: {
    display: "flex",
    alignItems: "flex",
    justifyContent: "flex",
    flexDirection:'row',
    padding: "10px",
  },
  container: {
    display: 'flex', 
    flexDirection: 'row'
  }
});

export default useStyles;
